import cartWidget from '../img/carritoCompras3.png';

const CartWidget = () => {
    return (
        <div style={{ backgroundColor: "white", color: "black"}}>
            <img src={cartWidget} alt="cart-widget"/>
            0
        </div>
    )
}

export default CartWidget